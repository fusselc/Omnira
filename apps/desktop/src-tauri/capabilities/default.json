{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability set for the main window. The webview can only reach what is listed here plus the app's own invoke commands (security layer 1, docs/local-security-boundary.md).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open"
  ]
}
