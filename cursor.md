# Cursor Guidance

Cursor-specific guidance for this repository. Treat `AGENTS.md` as authoritative for project rules.

## Working Rules

- Read and follow `AGENTS.md` before making project changes.
- Keep edits scoped to the requested task.
- Prefer the existing stack and repository patterns.
- Avoid speculative post-MVP features unless explicitly requested.
- Do not duplicate or override canonical project rules here except for Cursor workflow notes.

## Cursor Notes

- Use `AGENTS.md` as the source of truth when agent instructions conflict.
- Keep generated code aligned with the MVP architecture: Tauri 2, React, TypeScript, Tailwind CSS, and Rust core.
- Preserve the local-first product boundary: no telemetry, accounts, cloud sync, model downloads, or default external calls.
